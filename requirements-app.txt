# Andy Clip — dependencias de la aplicación local (backend FastAPI).
# Incluye el core y el modo local completo.
-r requirements-local.txt

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
